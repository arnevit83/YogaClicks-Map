@model Clicks.Yoga.Portal.Models.Venues.VenueStylesUpdateModel

@using (Html.BeginForm("UpdateStyles", "Venues", FormMethod.Post, new { enctype = "multipart/form-data", id = "update-styles-form" }))
{
    @Html.HiddenFor(m => m.VenueId)

    @Html.EditorFor(m => m.Styles)

    <div class="venue-services-save-section">
        <span class="venue-styles-saved-text">Changes Saved</span>
        <button type="submit" class="button save fr">
            Save changes &nbsp;
            <img src="/images/icon-save.png">
        </button>
    </div>
}