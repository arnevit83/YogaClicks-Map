@model Clicks.Yoga.Portal.Models.Entities.ImageModel

@{
    Layout = null;
}

@if (Model != null && Model.Exists)
{
    @Html.Image(Model, i => i.Resize(c => c.Width(266).Height(118).ScaleBoth().Crop()))
}
